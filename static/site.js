const archData={
 registry:{tag:'01 / IDENTITY',title:'AI Asset Registry',copy:'Give every autonomous AI system a durable identity across versions, environments and owners. Identity becomes the anchor for every assurance record.',points:['Immutable asset/version lineage','Environment and ownership context','Machine-readable identity']},
 graph:{tag:'02 / DEPENDENCIES',title:'Dependency Graph',copy:'Map models, tools, services and authority relationships so a change anywhere can trigger the right assurance response.',points:['Model and tool relationships','Version-aware dependency edges','Impact propagation']},
 evidence:{tag:'03 / EVIDENCE',title:'Evidence Fabric',copy:'Collect and verify evidence from evaluations, security checks, policy decisions and runtime signals without tying the system to one AI vendor.',points:['Canonical evidence records','Integrity and provenance','Vendor-neutral inputs']},
 trust:{tag:'04 / TRUST',title:'Trust Computation',copy:'Convert evidence and policy into a persistent assurance state with deterministic transitions and an inspectable history.',points:['ASSURED / DEGRADED / BLOCKED','Policy-driven computation','Historical state transitions']},
 enforcement:{tag:'05 / ENFORCEMENT',title:'Runtime Enforcement',copy:'Make assurance operational. Deployment and runtime controls consume trust state and return an explicit machine decision.',points:['ALLOW / REVIEW / DENY','Deployment gates','Runtime authorization']}
};
const $=s=>document.querySelector(s);document.querySelectorAll('.arch-item').forEach(el=>el.addEventListener('click',()=>{document.querySelectorAll('.arch-item').forEach(x=>x.classList.remove('active'));el.classList.add('active');const d=archData[el.dataset.key];$('#archTag').textContent=d.tag;$('#archTitle').textContent=d.title;$('#archCopy').textContent=d.copy;$('#archPoints').innerHTML=d.points.map(x=>`<div class="point">${x}</div>`).join('')}));
const state={ASSURED:{score:98.7,decision:'ALLOW · DEPLOYMENT AUTHORIZED',widths:[99,98,97,100]},DEGRADED:{score:76.4,decision:'REVIEW · HUMAN APPROVAL REQUIRED',widths:[91,82,69,86]},BLOCKED:{score:41.2,decision:'DENY · DEPLOYMENT BLOCKED',widths:[61,42,38,25]}};
function setState(name){const s=state[name];document.querySelectorAll('[data-state]').forEach(b=>b.classList.toggle('active',b.dataset.state===name));$('#trustScore').textContent=s.score.toFixed(1);$('#decision').textContent=s.decision;document.querySelectorAll('.bar i').forEach((b,i)=>b.style.setProperty('--w',s.widths[i]+'%'));$('#trustRing').style.background=`conic-gradient(${name==='ASSURED'?'#43e3a1':name==='DEGRADED'?'#f6c76b':'#ff6b7a'} 0 ${s.score}%,rgba(255,255,255,.07) ${s.score}% 100%)`;}
document.querySelectorAll('[data-state]').forEach(b=>b.addEventListener('click',()=>setState(b.dataset.state)));
const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('show')),{threshold:.08});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));
